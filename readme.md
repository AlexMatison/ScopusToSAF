## Scopus to Simple Archive Format
Runs a scopus search, extracts the article details, generates items in SAF format.
Set the SCOPUS_API_KEY and SCOPUS_SEARCH_STRING environment variables as required.
